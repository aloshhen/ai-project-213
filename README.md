# WEBSITE Project

> 🤖 Этот проект был автоматически сгенерирован с помощью AI Constructor Bot

## 📝 Описание

Отличная идея. Это идеальный тест для Kimi, потому что задача требует точной координации между JavaScript (отслеживание курсора, скролл) и CSS (режимы наложения, инверсия).

Соннет 4.5 часто справляется с этим "творчески", иногда усложняя код. Посмотрим, сможет ли Kimi 2.5 выдать более чистое и производительное техническое решение.

Вот детальный промпт, заточенный под логику Kimi, описывающий тот самый архитектурный концепт.

🏛 Промпт для Kimi: "The Inverse Architect — Brutalist Interactive Site"
Роль: Ты — ведущий фронтенд-архитектор с экспертизой в креативном кодинге и бруталистском дизайне. Твоя задача — создать технически безупречный, интерактивный лендинг для архитектурного бюро.

Стек и Ограничения:

React + TypeScript + Tailwind CSS.

Анимации: Framer Motion (для плавности появления и переходов).

Иконки: ИСКЛЮЧИТЕЛЬНО через компонент <SafeIcon name="kebab-case" />. Прямой импорт из библиотек запрещен.

Мобильная адаптация: Критически важна. На тач-устройствах кастомный курсор должен отключаться, возвращая стандартное поведение.

Ключевые Технические Механики (The Core Features):
1. Кастомный Курсор "Точка" (The Blend-Mode Dot):

Скрой стандартный курсор браузера (cursor: none для body).

Создай компонент CursorDot: фиксированный div размером 12x12px, скругленный на 50%.

Критическая логика цвета: Курсор должен иметь белый цвет фона и CSS-свойство mix-blend-mode: difference;.

Поведение: С помощью React state и ивента onMouseMove отслеживай координаты мыши и перемещай эту точку. Добавь небольшую задержку (лаг) через Framer Motion spring для плавности.

Важно: Убедись, что у точки стоит pointer-events: none;, чтобы она не перекрывала клики.

2. Инверсия Цвета при Скролле (The Scroll Inversion Effect):

Сайт должен состоять из чередующихся полноэкранных секций с контрастными темами.

Секция 1 (Dark): Фон черный (bg-black), текст белый (text-white).

Секция 2 (Light): Фон белый (bg-white), текст черный (text-black).

Механика: Когда курсор с mix-blend-mode: difference находится на черной секции, он выглядит белым. Когда он переходит на белую секцию, он автоматически инвертируется и выглядит черным. Это создает эффект "рентгена".

Структура и Дизайн Контента:
Общая эстетика: Монохромный брутализм. Огромная типографика, минимум декора, строгая сетка.

Hero Section (Dark Theme):

Огромный заголовок по центру: "VOID / STRUCTURE". Шрифт массивный, жирный, без засечек.

Подзаголовок: "Radical architecture for a post-digital world."

Featured Projects (Light Theme - Inversion happens here):

Заголовок секции: "SELECTED WORKS".

Сетка проектов (2 колонки). Каждая карточка — это большое черно-белое фото здания.

Hover эффект: При наведении на карточку проекта, фото слегка увеличивается, а рядом появляется иконка стрелки (используй <SafeIcon name="arrow-up-right" />). Курсор-точка над фото должен инвертировать цвета изображения под собой.

Philosophy / About (Dark Theme):

Текстовый блок с манифестом бюро. Крупный шрифт, выключка по левому краю.

Используй иконки для списка принципов: <SafeIcon name="layers" /> (Materiality), <SafeIcon name="maximize" /> (Scale), <SafeIcon name="zap" /> (Impact).

Contact (Light Theme):

Огромная ссылка на почту внизу страницы.

## 🚀 Технологии

React, TypeScript, Tailwind CSS, Framer Motion

## 📁 Структура проекта

- `package.json`
- `index.html`
- `vite.config.js`
- `tailwind.config.js`
- `postcss.config.js`
- `src/main.jsx`
- `src/index.css`
- `src/App.jsx`
- `src/components/IconRegistry.tsx`
- `src/components/SafeIcon.tsx`
- `src/globals.css`
- `vercel.json`
- `.gitignore`
- `README.md`

## 🛠️ Установка

```bash
npm install
```

## ▶️ Запуск

```bash
npm start
# или
npm run dev
```

## 📋 План разработки

1.

## 📄 Лицензия

MIT

## 🤖 Создано с помощью

[AI Constructor Bot](https://t.me/construct_ai_bot) - Telegram бот для автоматической генерации проектов с помощью AI
